export interface registerByEmailReturnedData {
    accessToken: string;
}

export interface registerByEmailInputData {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}